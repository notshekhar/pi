{
  "name": "@notshekhar/pi",
  "version": "0.0.2",
  "description": "pi — terminal coding agent. Multi-provider, fast, native TUI.",
  "license": "MIT",
  "type": "module",
  "homepage": "https://github.com/notshekhar/pi#readme",
  "bugs": { "url": "https://github.com/notshekhar/pi/issues" },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/notshekhar/pi.git",
    "directory": "packages/cli"
  },
  "keywords": ["ai", "agent", "cli", "tui", "coding", "claude", "cursor", "gpt", "grok", "gemini", "openrouter", "pi"],
  "bin": {
    "pi": "./dist/cli.js",
    "agent": "./dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup && chmod +x dist/cli.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "^0.74.1",
    "@earendil-works/pi-tui": "^0.74.1",
    "@notshekhar/pi-core": "*",
    "chalk": "^5.5.0"
  },
  "devDependencies": {
    "tsup": "^8.3.0"
  }
}
